# sqlite3 是 @cursor/sdk 的间接依赖、安装脚本 `prebuild-install -r napi` 默认从 GitHub releases 拉预编译二进制
# 国内网络经常超时 → 退回 node-gyp 源码编译 → 需要本机装 Visual Studio C++ 工具链（很重、新人踩坑）
# 这里把 binary host 指到淘宝镜像（win/mac/linux 全平台 napi 包都有）、安装时自动命中、不再需要编译
# 注意：prebuild-install 7.x 按 package name 读环境变量、所以是 sqlite3_binary_host_mirror、
#       不是老 node-pre-gyp 的 node_sqlite3_binary_host_mirror（那个对 6.x 无效、网上教程多数过时）
sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/sqlite3
