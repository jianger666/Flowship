---
description: UI / UX 交互规范（导航、链接、反馈、设计 skill 用法）
alwaysApply: true
---

# UI / UX 交互规范

> V0.7.8 全站 UX review 后立规。改 UI / 加页面 / 加交互前先过一遍本文件；新增规范追加到对应小节、不要另开文件。

## 导航 / 返回（2026-06-12 用户拍板）

- **每个二级页面 / 全屏视图必须有显式「返回」**——不能让用户只能点 logo 回首页。已覆盖：task 详情（task + chat 两种模式）、设置页。新加页面照办。
- **返回语义 = 回来路**：
  - 入口固定的页面（task 详情只从首页进）→ 返回写死父级（`href="/"`）
  - 入口多样的页面（设置页从首页 / 任务详情都能进）→ `router.back()`、无历史（直开 URL）兜底 `router.push("/")`、参考 `settings/page.tsx` 的 `handleBack`
- 同形态的页面顶部条保持同款结构（返回按钮 + Separator + 标题 + badge）、别一个页面一个样。

## 链接行为（V0.7.7/V0.7.8 定型）

- **markdown 正文里的链接**（AI 回复 / artifact）一律走 `src/components/markdown-link.tsx`：
  - http(s) → `target="_blank"`（浏览器开新 tab、Electron 壳拦 window.open 转系统默认浏览器）
  - 相对路径 / file:// 等 AI 幻觉链接 → 等宽纯文本不可点（点了必 404、误导用户）
  - 新增 ReactMarkdown 实例必须配 `components={{ a: MarkdownLink }}`
- **JSX 里手写的外链** → `target="_blank" rel="noopener noreferrer"`（MR 链接、飞书链接等）
- **cursor:// 跳 IDE** → 同 frame 裸 `<a>`（壳 will-navigate 拦截转系统、浏览器原生唤起协议、页面都不动）
- **Electron 壳 `setWindowOpenHandler` 零白名单**：所有 window.open 一律 deny + `shell.openExternal`——不要再加「站内 URL 开子窗」之类的 allow 分支（用户实测「应用内闪一下」体验差、v0.7.8 拍板全走系统浏览器）

## Dialog 关闭行为（2026-06-12 用户拍板）

- **带表单草稿的 dialog 一律 `disablePointerDismissal`**（点弹窗外不关、Esc / X / 取消仍可关）——典型踩坑：新建任务弹窗里关「目标仓库」下拉要点空白、一点把整个弹窗带没、草稿全丢。已覆盖：new-task-dialog / advance-dialog / revise-dialog。新加带输入的 dialog 照办。
- 纯展示 / 确认类 dialog（无草稿可丢）保持默认点外可关。

## 文件 / 目录选择（2026-06-12 用户拍板「全部用原生、这是大方针」）

- **一律走 `pickNativePaths`**（`src/lib/native-picker.ts`）：桌面端 Electron 壳走主进程 `dialog.showOpenDialog`（秒弹 + 自动前台聚焦）。项目只交付桌面端、非桌面环境直接 toast 提示、不再写自绘文件浏览器或 HTTP 兜底。
- 调用期间给按钮 spinner（防双击连开两个对话框、参考 event-stream 附文件按钮）。

## 选中态颜色（v1.0.x 立规、用户实测「白色的选中状态太弱」+ 拍板「沉淀到全局规范」）

- **持久选中态**（当前 tab / 当前导航项 / 侧栏激活行 / 下拉里已选项 / 主题选中项等「离开后还在」的状态）一律用全局 token：`bg-selected text-selected-foreground`（globals.css `--selected` / `--selected-foreground`、浅深两套已调好对比度）。
 - ⛔ 不要再自拼 `bg-muted/60`、`bg-accent/40` 这类半透明灰当选中态——浅色模式下叠在白底上肉眼几乎不可见（踩过：能力页 tab / 设置页导航 / 下拉已选项全中招）。
- **瞬时态**（hover / 键盘高亮 / drag-over）保持 shadcn 惯例：`hover:bg-accent`、`hover:bg-muted/50` 等——转瞬即逝、弱一点没关系、跟选中态拉开层次。
- 需要「品牌强调」的选中（ChoiceButton card/chip）继续走 `border-primary + bg-primary/5~10 + text-primary`（有边框和文字色双重信号、两种模式都可辨）。
- 改 `--selected` 值只动 globals.css 一处、所有选中态全站跟随。

## 语义状态色 token（2026-07-28 一致性收口立规）

- **一族一个 token，组件里不许再出现裸 Tailwind 色号**（`emerald-600` / `sky-500` / `amber-500` / `red-500` …）。token 在 `globals.css` 定义（`:root` + `.dark` 两套）、`@theme inline` 映射，做法与 `--selected` 完全同款：

  | token | 语义 | 典型用法 |
  | --- | --- | --- |
  | `--success` | 成功 / 已完成 / 连通 | `text-success`、`bg-success/10`、`bg-success`（状态点） |
  | `--warning` | 注意 / 有风险但不阻塞 | 已截断、非默认模型、缺项引导、探测「连上了但不对劲」 |
  | `--info` | 进行中 / 中性信息 / 可跳转 | 运行中 spinner、tool_call、待办卡、路径链接、来源标 |
  | `--destructive` | 失败 / 危险（**沿用现成的、不新增 danger**） | 错误图标、失败状态点、diff 删除行 |
  | `--brand` | 品牌琥珀「电流」＝**唯一 accent** | logo、「等你行动」信号族 |
  | `--subagent` | 块类型身份色（**不是状态色**） | 事件流子代理卡 / 思考图标 |

- **取值口径**：浅色统一压到 `L≈0.54~0.58`（跟 `--destructive` 的 0.577 同档、白卡上 4.7~5.5:1）；深色统一 `L≈0.76~0.82`（原来在用的 400/500 档、8.3~9.9:1）。改颜色只动 `globals.css` 一处。
- **「运行中」全站只有一种颜色：`text-info`**。`muted-foreground` 已经被「已中断」和降权过程行占着，运行中再用它就分不出两个状态了。
- **`--brand` 只给「等你行动」**（侧栏琥珀脉冲点 / ask 答题卡 / 「AI 在等你回答」悬浮条 / `ask_user_request` 图标）+ logo。⛔ 别拿琥珀去染装饰性图标（目录 icon 之类）——每多染一处，真正需要用户行动时的信号就弱一分。装饰性图标一律 `text-muted-foreground`，形状本身已经能区分。
- **`--warning` 和 `--brand` 同源不同档**（hue 80 vs 55），语义分开：warning = 「你看一眼」，brand = 「等你动手」。
- 「当前查看 / 选中」永远走**选中态规范**（`bg-selected` + `ring-ring`），⛔ 不许为某一处单独引入第三种彩色体系（踩过：action-timeline 的 indigo 是全站唯一一处靛蓝）。

## 字号阶梯：只有 4 档（2026-07-28 立规）

收口前一屏并存六档（10 / 11 / 12 / 12.8 / 14 / 15），相邻档差 1px——起不到层级作用，只让同类信息「差一点点」，这是「看起来粗糙但说不出为什么」的典型来源。

| 档 | 写法 | 用途 |
| --- | --- | --- |
| 15px | `text-[15px] leading-7` | 对话正文（chat 的 AI 回复；流式 placeholder 必须同档、否则收尾时整段跳一下） |
| 14px | `text-sm` | 默认正文 |
| 12px | `text-xs` | 辅助文字、次要说明、小按钮 |
| 11px | `text-[11px]` | 元信息：时间戳、tag、计数、状态尾巴 |

- ⛔ **不许再出现 10px**（可读性差一档、省不出信息密度）、`text-[0.8rem]`（12.8px）、`text-[12px]`（重复拼法、统一写 `text-xs`）、`text-[13px]`。
- 12 和 11 之间不要再插新档；需要「比 11 更弱」用透明度（`text-muted-foreground/70`）而不是更小的字。

## 页面标题只有 2 档（2026-07-28 立规）

- **独立页**（看板 / 设置 / 能力 / 开始使用）：`text-lg font-semibold`
- **详情页顶栏**（task 详情 / chat 详情）：`text-sm font-medium tracking-tight` —— 详情页顶栏是工具条不是封面，标题让位给密度；task 和 chat 是同一层级、必须同一规格。

## 胶囊 / tag 家族（2026-07-28 立规）

三种形态、各有归属，⛔ 不许再手写第四种：

- **徽标** → `<Badge>`（`rounded-4xl`）。两档尺寸：`size="default"`（h-5 / 12px，独立状态标）、`size="xs"`（h-4.5 / 11px，挤在行内的元信息标）。语义色走 `variant="success" | "warning" | "destructive"`（三者形状对称、都是 `/10` 底衬 + 同色字）。⛔ 别在调用处用 `className="px-1 py-0 text-[11px]"` 或自己拼 tone class 造第三档。
- **可点选 chip** → `<ChoiceButton shape="chip">`（`rounded-full`）。
- **行内 tag**（action 归属标、来源标、待办 / 子代理标、skill / file token、muted 小计数标）→ 统一 `rounded-sm` + 11px。事件流的 action 归属标已抽成 `ActionTag`（`event-stream/utils.tsx`）、三处共用，别再各写一份。

## 设置类页面（v1.0.x 立规、用户两轮实测「乱」后定型）

- **每个配置项一律用 `<SettingRow>`**（`src/components/ui/setting-row.tsx`）：左「名称 + 一句说明」右控件；宽控件（Input / ModelSelect）用 `stacked`（名称行 + 全宽控件、`labelExtra` 放名称行右侧操作按钮）。外层容器 `divide-y` 出行间分隔。
- ⛔ 不要手拼「小节头 + Label + 控件 + 说明」多层文字——三层文字层级挤在一起就是「乱」的根源（踩过两轮）。
- **禁 placeholder-only 输入**：并排多个同类输入（如三个分支框）必须带迷你 label（`text-[11px] text-muted-foreground`）——有值后 placeholder 消失、用户就不知道哪个框是啥了（ui-ux-pro-max 规范同款）。
- 设置分组按「用户心智」不按实现：连接（外部服务凭据）/ 偏好（个人习惯）/ 仓库 / 存储；能力类（Action / Skill / MCP）在 /actions 能力页。

## 操作反馈（已有约定、汇总）

- 加载中 → `LoadingState`；空态 → `EmptyHint`；选中卡片 → `ChoiceButton`（见 learned-conventions）
- 不可逆操作（删除 / 放弃）→ `useDialog().confirm` + `destructive: true`
- 异步动作期间按钮 disabled + spinner（参考 chat-view 「停止」按钮）、防双击连点
- 结果反馈 → `toast.success` / `toast.error`、错误信息带上下文（`操作失败：${message}`）

## 键盘可达性

- 可点击卡片不要只挂 div onClick——主要动作给真 `<Link>`（参考 task-card 标题）、键盘 tab 可达
- 弹窗内表单 Enter 提交、Esc 行为遵循 use-dialog 既定约定（不允许 Esc 误关 confirm）
- **「输入框 / textarea 提交快捷键」一律走设置页偏好、不要写死 Enter / Cmd+Enter**（V0.8.8 立规）：
  - 用 `useSubmitShortcut()`（`@/hooks/use-settings`）拿当前偏好 + `shouldSubmitOnKeyDown(e, shortcut)`（`@/lib/submit-shortcut`、已处理 IME / enter vs mod-enter）判定。现成例子：event-stream / advance-dialog / revise-dialog / ask-user-dialog。
  - 多焦点弹窗（如 ask_user 有选项按钮 + textarea）把 handler 绑在 `DialogContent` 容器级（事件冒泡覆盖 textarea）；但 `enter` 模式要 guard `e.target.tagName === "TEXTAREA"`、只在 textarea 内裸 Enter 提交、避免焦点在按钮 / 容器时裸 Enter 误提交整表。
  - 例外：**单行 `<Input>`**（如 `useDialog().prompt` 重命名框）保持 Enter 提交即可——设置项本意是「多行 textarea 的 Enter 歧义」、单行无歧义、不套设置。

## 滚动条布局抖动全局治理（2026-07-23 用户拍板「全局治理、以后新增也不复现」）

- 自定义滚动条是 classic 模式（占 8px 布局宽度）→ 滚动条出现/消失会挤压内容宽度、整页抖动（踩过：能力页切 tab 抖）。
- **治理机制在 `globals.css`**：`scrollbar-gutter: stable` 挂在 `.overflow-y-auto` / `.overflow-auto` 工具类上——新增滚动容器只要用这两个 Tailwind 类就自动带治理、不需要手动加。
- ⛔ 不要在单个组件里手加 `[scrollbar-gutter:stable]` 打补丁；⛔ 不要把规则改成 `*` 全局（overflow-hidden 容器会白吃 8px 槽位）。

## 输入框「可点区」与 footer 挤压（2026-07-28 用户实测两条一起立规）

- **定高的编辑区、编辑器本体必须撑满外层容器**（`min-h-full`）——外层给了固定高度（推进弹窗 104 / ask 答题卡 84 / 输入条拖高值）而编辑器只有 `min-h-13` 时，下面那截空白属于外层 div、点了不聚焦（用户实测「指令框只有顶上一行能点」）。高度随内容（无固定高）的场景保持 `min-h-13` 即可——外层本来就是被编辑器撑起来的、没有空档。⛔ 别用「外层 onClick 手动 focus」绕：会干扰选词、光标落位也不自然。
- **`min-w-0` 要加在「自己是 nowrap 内容」的那个 flex item 上、父级加了不算数**——`truncate` 自带 `white-space: nowrap`，该元素的 min-content = 整串文字宽；它作为 flex item 时 `min-width:auto` 就等于内容宽、`max-w-*` 也压不住（min 赢 max），于是溢出父容器压到同排的右侧控件上（踩过：composer footer 模型选择器压住附图 / 发送图标）。父容器的 `min-w-0` 只让父容器能缩、管不到子元素。
- 给别人塞内容的 **slot 容器**（footer `leading`、dialog body 之类）统一挂 `*:min-w-0` 兜底（同 `DialogContent` 既有做法）、新加内容不必各自记得。

## 设计 skill（V0.7.8 引入、两个都在 .cursor/skills/）

- **新页面 / 新视觉块 / 改版**：先读 `frontend-design`（防 AI 模板脸、定调性）+ 用 `ui-ux-pro-max` 检索具体建议（`python3 .cursor/skills/ui-ux-pro-max/scripts/search.py "<query>" --domain style|color|typography|ux`）
- 日常小改（加按钮 / 调间距）不必过 skill、遵守本文件 + learned-conventions 即可
- 用户对 UI 的每次纠偏反馈 → 沉淀到本文件或 learned-conventions（用户拍板「期望 AI 慢慢沉淀」）
