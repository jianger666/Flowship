---
description: 调用子代理禁止使用 fast 模型（Composer 2.5 Fast 约 6 倍价）
alwaysApply: true
---

# 子代理禁止 fast（2026-08-18 用户拍板）

主线用 Task / 任何子代理入口派活时，**不允许选用 fast 变体**。

- **计费事实**：Composer 2.5 Fast 是非 Fast 的约 **6 倍**价，不是更便宜。slug 里带 `fast` 的都当贵模型，不要为了「快」去选。
- **默认**：`model` 用 `inherit`（跟主线同一模型）。用户没点名具体模型时不要另填。
- **禁止传入**（当前列表里已出现的、以及以后新加的同款）：`composer-2.5-fast`、`cursor-grok-4.5-high-fast`，以及任何 `*-fast` / 参数 `fast=true`。
- **允许列表里如果只有 fast 版、没有非 fast 对照**（例如只有 `composer-2.5-fast`、没有 `composer-2.5`）→ **不要选它**，改 `inherit` 或其它非 fast slug（如 `cursor-grok-4.6-xhigh` / `kimi-k3-max`）。
- **专用类型也会默默走 fast**：`bugbot` 等子代理类型若内部默认 Composer 2.5 Fast，**必须显式传非 fast 的 `model` 覆盖**；覆盖不了就不要用该类型，改 `generalPurpose` + `inherit`，或主线自己审。
- 用户口头指定「用某某 fast」才可以破例；没说就禁。
