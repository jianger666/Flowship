---
description: 从对话反馈中积累的编码约定和偏好
alwaysApply: true
---

# 编码约定

## 本地测试桌面 app：用 test 实例、别碰 8876（2026-06-12 用户修订）

- **用户本机日常运行着「AI工作流」桌面 app（8876 端口 + `~/Library/Application Support/fe-ai-flow`）当生产环境用**——禁止 curl 8876 / 杀 8876 进程 / osascript 关「AI工作流」窗口。
- **可以**本地起测试实例验证、但必须走隔离三件套（v0.7.9 已实装）：
  - **⚠️ 打 test 包前必须先退掉正在跑的 test 实例（2026-07-02 踩过、把用户测试环境搞半残）**：`electron:dist:test` 会把 `dist/electron/mac-arm64/AI工作流test.app` **整个删掉重建**——老进程还在跑时脚下 bundle 被换、已加载的路由还能服务（boot 200、curl 冒烟全过、**假象**）、但之后 agent 每次执行 shell、SDK 沙箱的 zsh state-dump helper 因 bundle 资源失配**永久挂死**（僵尸 `/bin/zsh -ilc dump_zsh_state` 进程、每跑一条 shell 挂一个）→ wait curl 发不出去、任务永远「运行中」假卡死。另一个坑：打完 `open` 只会聚焦老进程（单实例锁）、**你以为在冒烟新包、实际测的是老进程老代码**。正确顺序：`osascript -e 'tell application "AI工作流test" to quit'` → 完整 3 步打包 → `open` → **核对主进程 lstart 晚于打包时间**再冒烟。
 - **正式包自更新点「稍后重启」踩的是同一个坑（2026-07-07 线上事故确诊）**：mac 自更新原地替换 /Applications 里的 .app、用户点「稍后」→ 老进程 + 新 bundle → 之后每个新 agent run 第一批 shell 就永久挂死、任务假死「运行中」。诊断特征：壳日志反复打 `发现新版本 vX（当前 vY）`（X = 磁盘版本 Y = 运行版本）、UI 右上角挂「新版本」徽标、next-server 下挂僵尸 `dump_zsh_state`。**V0.10.1 起有硬闸**：壳替换后写 `<data>/update-pending-restart.json`、server 起新 run 前查到直接拒绝提示重启（`update-pending.ts`）；再遇到「任务卡死 + 事件流停在 shell 调用后」先查这个。修复 = 让用户重启正式 app（我不能动 8876）。
 - 打包（**完整 3 步、缺一不可**、2026-06-15 踩过：只跑 dist:test 会把旧代码打进包、验证时 curl 到旧 UI 排查半天）：
    1. `BUILD_STANDALONE=1 pnpm build`——产新 `.next/standalone`（不带 BUILD_STANDALONE 不会出 standalone、见 next.config.ts）
    2. `node scripts/assemble-electron-server.mjs`——从 standalone 重组 `dist/app-server`（内部先 rm 再 cp + 补 SDK 平台包）
    3. `pnpm electron:dist:test`——afterPack（`scripts/electron-after-pack.mjs`）把 `dist/app-server` 拷进「AI工作流test.app」（productName 带 test）+ ad-hoc 签名
    ⚠️ `electron:dist:test` **只拷不组**：afterPack 源固定是 `dist/app-server`、自己不 build / 不 assemble。漏掉第 1、2 步 → 打进包的是上一次的旧 `dist/app-server`（产物 mtime 会骗你说是新的、但内容是旧的）。核验：`grep -c <本次新增/删除的文案> "dist/electron/mac-arm64/AI工作流test.app/Contents/Resources/app-server/.next/server/app/index.html"`。
  - 壳探测到名字带 test（或 env `FE_AI_FLOW_TEST=1`）自动切：端口 **8776**、数据目录 **fe-ai-flow-test**、单实例锁天然独立（按 userData 算）
  - 要关 / 删的话（用户主动说时）动的也是「AI工作流test」（名字带 test）、不会误伤正式应用
- 验证写法：`open dist/electron/mac-arm64/AI工作流test.app` + `curl 127.0.0.1:8776/...`。**验完默认不关窗、不删包**（见下条）。
- **⚠️ 重打包后 TCC 文件权限会重置、锁屏时验 task 模式必假卡死（2026-07-07 踩过、排查 1 小时）**：ad-hoc 签名每次重打都变、macOS 把新包当新 app、agent 子进程首次访问 `~/Documents` 下的仓库要重新弹「文件与文件夹」授权——**用户不在电脑前 / 锁屏时弹窗出不来、系统调用直接永久挂起**（采样实锤：git 挂在 `getcwd→open` 内核调用、Agent.create 卡 20 分钟无报错）。表现三件套：chat 模式正常（cwd=home 不受限）、独立终端跑 SDK 正常（终端有权限）、只有 app 里 task 模式挂。规避：① 冒烟 task 模式必须屏幕解锁、人在电脑前点掉授权弹窗；② 一劳永逸——系统设置→隐私与安全→完全磁盘访问 给「AI工作流test」勾上；③ 绕开法：用 `/tmp` 下的临时仓验链路（不触发 TCC）、但 skills 在 app bundle（Documents 下）里、agent 读 skill 仍会卡——治标不治本、优先 ②。
- 盲写直接发版（不本地验证）也不可取——能 test 实例验的就验一轮再 tag。
- **test 实例 + test 包默认都不关不删（2026-06-15 用户修订、覆盖旧「测完删包 / 立即关 test」约定）**：验完 test 实例**留着跑、test 包（`dist/electron`）也留着**、下次要再测直接 open 复用、不用重打。**只有用户主动说「关」「删」时才动手**。配置数据目录 `~/Library/Application Support/fe-ai-flow-test`（API Key / 模型 / 仓库 / 飞书登录态）一直保留、不要删。
- **⚠️ test 和正式不能同机同时跑 SDK run（2026-06-13 把用户生产搞挂、踩过）**：两个独立 app 进程（test 8776 + 正式 8876）各跑各的 cursor-agent、抢同一个本机 `~/.cursor` session 初始化 / 文件锁 → race（[官方已知 bug](https://forum.cursor.com/t/concurrent-headless-cursor-agent-invocations-fail-without-delay/142677)、表现一个正常一个秒 `SDK status=ERROR message=(none)`）。**同进程内多 agent 不受影响**（正式包内并行多任务一直 OK）、**跨进程才撞**。所以：**起 test 跑对话前、确认正式实例此刻没在跑对话**（idle 挂着不跑 SDK 不会撞、所以 test 默认留着即可、不必为规避并发去关它）。真把正式挤挂了：重启正式 app 即恢复（重置 SDK 会话态）。
- **test 包自动补 SDK 平台包（v0.7.17、assemble-server.addSdkPlatformPackage）**：`electron:dist:test` 走 standalone、Next nft 追不到 `@cursor/sdk-<platform>` 运行时动态加载的平台子包（native cursorsandbox + rg）→ 缺了 SDK 直接 `fetch failed`、对话全挂。组包公共函数已自动补（本地 pnpm 拓扑拷平台包 + symlink、CI hoisted 跳过）——打完可 `ls .../app-server/node_modules/.pnpm/@cursor+sdk@*/node_modules/@cursor/` 确认有 `sdk-darwin-arm64` symlink。
- **同步正式配置到 test**：`scripts/sync-test-config.mjs`（需 `classic-level`、按需 `pnpm add -D classic-level`）。配置存 Chromium localStorage（`Local Storage/leveldb`）、按 origin（含端口）隔离、正式 8876 ≠ test 8776 直接拷读不到。流程：cp 正式 `Local Storage/leveldb` → `/tmp/fe-prod-ls`（正式 app 在跑、有 LOCK、只能读副本）→ test app 先退出（leveldb 单写锁）→ 脚本读副本、把 key 里 origin 端口 `8876→8776` 转换写入 test（`--probe` 只读看 / `--verify` 读回校验 / 无参正式同步）。
- **删依赖要同步清 CI 打包链 + lockfile（v0.8.11 踩过、连挂 4 版才发现）**：`release.yml` 的 electron job「修平台依赖」步会 hardcode 按目标平台重装「nft 追不到、运行时动态加载」的原生包。**移除某依赖时若这步还在探测它 → standalone 产物里没有 → `node -p require('./node_modules/<pkg>/package.json')` 第一行直接炸、win+mac 两个 job 全挂、release 永远卡 draft**。典型 sqlite3：早成死依赖（源码 0 引用 / lockfile 仅剩 overrides 声明、零实际包条目 / standalone 不含 / test 包一直能 boot 证明运行时不需要），但 CI + `package.json`（overrides + onlyBuiltDependencies）没同步清 → v0.8.7~v0.8.10 打包全失败。删依赖三件套：① 清 `package.json` 依赖 / overrides / onlyBuiltDependencies；② 清 `release.yml` 里对该包的探测 / install / require 验证；③ `pnpm install` 同步 lockfile（CI `--frozen-lockfile` 要求一致、本地先跑一遍 `pnpm install --frozen-lockfile` 校验过）。
- **查 GitHub Actions 失败原因（本机 `gh` 没装）**：用 `TOKEN=$(printf "protocol=https\nhost=github.com\n\n" | git credential fill | sed -n 's/^password=//p')` 取本地存储凭据（helper=store）、再 `curl -H "Authorization: Bearer $TOKEN"` 调 Actions REST API：`/actions/runs` 列 run、`/actions/runs/<id>/jobs` 看哪步挂、`/actions/jobs/<id>/logs` 下日志 grep 报错。匿名调有 rate limit、必须带 token；token 只进变量 / `-H`、别 echo 出来。

## 验证不主动截图给用户（2026-06-15 用户拍板）

- **验证时别把截图塞进 feedback summary 给用户看**——用户日常自己盯着运行的 test / 正式 app、效果他自己看得到、不需要我贴图。
- 截图只用于「我自己确认改动生效」（自查），不是给用户的交付物；验证靠自己跑通即可（typecheck / lint / curl、必要时自己截图自检）。

## UI 组件库

- **统一用 shadcn/ui**——所有交互元素（button / input / select / textarea / card / dialog / toast 等）走 shadcn、不要手写原生 element 拼 className。
- 页面尽量由组件组合而成、避免在 page.tsx 里直接堆 `<input>` `<button>` 等原始标签。
- shadcn 组件放在 `src/components/ui/`、业务组件放在 `src/components/`。

## 禁用浏览器原生弹窗

- **`window.alert` / `window.confirm` / `window.prompt` 一律禁用**——dark 主题下白底白字、带浏览器 chrome、风格违和、还会阻塞主线程。
- 替代方案（已封装好）：
  - **确认 / 二次确认** → `const { confirm } = useDialog()`、`await confirm({ title, description, destructive, ... })`、返 `Promise<boolean>`。
  - **输入框 prompt** → `const { prompt } = useDialog()`、`await prompt({ title, defaultValue, validate, ... })`、返 `Promise<string | null>`、`null` 表示用户取消。
  - **toast 通知**（不需要交互）→ `import { toast } from "sonner"`、`toast.success(...) / toast.error(...)`。
- hook 实现：`src/hooks/use-dialog.tsx`、Provider 已挂在 `src/components/providers.tsx`、所有客户端组件可直接 `useDialog()`。
- 写新代码 / 改老代码遇到 `window.alert/confirm/prompt`——直接换成上面三选一。

## 函数声明

- **统一用箭头函数**——避免 `function foo() {}` 形式；
  - 模块顶层函数：`const foo = () => { ... }` 或 `export const foo = (...) => { ... }`
  - 组件：`const Page = () => { ... }`、`export default function` 仅在 Next.js 必须 default export 时用得无奈、能换就换
- 现成的 shadcn 等第三方代码保留它原本的 `function` 声明、不要为了规则去改第三方文件。

## 注释风格

- **用户偏好"代码多写点注释方便他看"**——优先写注释解释：
  - 这段代码"为什么"这么写（业务背景、约束）
  - 关键状态、副作用、易踩坑的边界
  - 模块顶部 / 函数顶部 / 复杂分支前一两句中文说明
  - **每个 useState / useRef / useMemo 等 state 声明、跟一行短注释说明这个状态是干什么的**
- 但避免**废话注释**（如 `// 调用接口`、`// 返回 null`），注释要有信息密度。
- 注释一律中文。

## UI 文案 / placeholder / 提示语简洁原则

- **placeholder / help text / 空态提示 / dialog 描述不要超过一行**——用户拿键盘说话不需要先读 5 行说明、产品文档不要写到输入框里。
- ❌ 反例（V0.5.10 踩过）：「再聊聊」textarea placeholder 写了 4 行教用户「纯疑问句 → 事件流答疑、其他 → 弹窗复述」、用户实测「冗余、系统该简洁」
- ✅ 正例：「想改、想问、或者贴图说明（支持粘贴 / 拖拽）」——告诉能干啥就够、AI 行为规则不用泄露给用户
- **不要把产品交互规则写进 placeholder**——规则在用户实际用过 1-2 次就理解了、塞进 placeholder 反而显得系统絮叨。
- **控件标题能自解释、就别再加一行 help text**（V0.6.14 用户拍板）——开关 / checkbox / 选项 label 的标题本身已说清「这是干啥的」、下面就别再缀一句「默认 X；想 Y 再开」。控件名 + 状态（开 / 关）用户看一眼就懂、补充说明纯属絮叨。
  - ❌ 反例：开关「合并后删除源分支」下面又写「默认保留；MR 合并后想自动删源分支再打开」——用户实测「这些可以不用写、系统里很多文案太啰嗦」
  - ✅ 正例：开关只留标题「合并后删除源分支」、默认值靠开关初始状态体现、不用文字解释
- 写一条提示前先问自己：「用户每次打开这个 dialog 都要读一遍这段话吗？」如果不是、就删。

## 共享常量 / 文案 / 工具的单一源

- **PHASE_LABEL / STATUS_LABEL / STATUS_VARIANT / formatRelative 等任务展示文案** → 统一放 `src/lib/task-display.ts`、不要在组件 / 页面里各自定义一份。
  - 改 phase 文案只改这一个文件、不会出现 task-card 显示「上下文 + 方案」、artifact-panel 显示「上下文 + 方案规划」这种漂移。
  - artifact-panel 那种「中文（英文）」复合标题在本文件里组合、不要污染基础表。
- **客户端「准备 SDK run 参数」逻辑** → 统一走 `src/lib/run-args.ts` 里的 `prepareRunArgs(task)`。
  - 任何「getSettings → 校验 apiKey/model → parseMcpServers → filterMcpServersByTask」组合都应该走它、不要复制粘贴这一段。
  - 校验失败 helper 内部 toast.error 后返 `null`、调用方 `if (!args) return;` 即可。
  - MCP 解析错误 prefix 通过 `options.mcpErrorPrefix` 切语境（启动 / 续接 / etc）。
- **`local-store.ts` export 的 `DEFAULT_SETTINGS` / `DEFAULT_MCP_JSON`** 是唯一来源、`use-settings.ts` / `mcp-card.tsx` 等不要再自己定义 `EMPTY_SETTINGS` / `EMPTY_MCP_JSON`。
- **`task-store.ts` 的 `handleJson<T>` 错误归一**——所有客户端 fetch 任务相关 API 应该通过 task-store 暴露的 helper 走、不要在组件里裸 `fetch + JSON.parse + 错误拼接`（参见 `submitAskReply` / `submitPhaseAck` / `addContextDoc` 等）。

## 共享 hook：SSE 订阅

- **任务事件流订阅一律走 `useTaskWatch(taskId, callbacks, enabled?)`**——别再自己写一遍 `AbortController + watchChatStream + AbortError 静默 + ref 化 callback` 那一套。
- enabled 开关用于「父子组件二选一订阅」场景（如 task page + ChatView 都能订阅同一个任务、但只让其中一方订）。
- 回调里需要拿最新 task / state 时、调用方自己用 ref 模式（hook 内部已经把 callbacks 集合做了 ref 化、不需要再 ref 化包一层）。

## 共享 UI：LoadingState / EmptyHint / ChoiceButton / ImageThumb

- **加载中**一律用 `<LoadingState variant="block" | "card" | "inline" />`、不要再写裸 `<div>加载中…</div>` 或自己拼 Card 包一遍。
  - block：页面级（默认占满 viewport 减顶 bar）
  - card：列表区位（首页任务列表 loading 用、视觉跟 TaskCard 对齐）
  - inline：行内小段落
  - 文案统一中文省略号「加载中…」、调用方需要的话覆盖 `label`。
- **空态提示**一律用 `<EmptyHint variant="dashed" | "solid" size="sm|md|lg" align="left|center" />`、不要重新写 `border border-dashed rounded-md ...`。
- **「可选中卡片 / chip / tab」交互按钮**一律用 `<ChoiceButton shape="card" | "chip" | "tab" selected={...}>`：
  - card：方块卡（mode 选择、option 选择块、可包多行内容）
  - chip：圆角小药丸（preset 标签、Other 切换）
  - tab：行内按钮（phase-progress 那种 hover 态切换）
  - 这是给「shadcn `<Button>` 不能直接套的自定义视觉选中态」准备的——不要自己拼 `cn("rounded border ...", selected && "border-primary")`。
- **图片展示**一律用 `<ImageThumb>`（缩略图、点击站内 lightbox 看大图）、**不要再手写 `<img>` 缩略图 / `<a target="_blank">` 跳系统浏览器**（V0.8.8 立规、用户痛点：原生 img 不能预览、新 tab 在 Electron 壳里跳出 app 体验差）。
  - 组件 + 全局 lightbox 都在 `src/components/ui/image-preview.tsx`：`ImageThumb`（缩略图）、`ImagePreviewProvider`（已挂 `providers.tsx`）、`useImagePreview()`（任意组件手动唤起预览）、`MarkdownImage`（markdown 内嵌图）。
  - 发送前预览传 `onRemove`（右上角显示移除 X）；同组多图传 `group: PreviewImage[]` + `index`、lightbox 内可 ←→ 切换 + 键盘。
  - **markdown 正文里的图（`![]()`）**走 `MarkdownImage`——新加任何 `ReactMarkdown` 实例的 `components` 必须配 `{ a: MarkdownLink, img: MarkdownImage }`（跟链接同等待遇、否则图退回不可预览的原生 `<img>`）。
  - 唯一例外：纯装饰 / logo（`/logo.png` 那种）不用 ImageThumb（不需要预览）。

## 关于 useEffect 依赖

- 不要随便加 `// eslint-disable-next-line react-hooks/exhaustive-deps`——大概率是真依赖漏了、会 stale closure。
- 例外：「故意只在 X 变化时重连」类场景、把 callback 做 ref 化（`callbackRef.current = callback`）后再放进 effect、然后依赖就只剩 X 一个、不需要 disable。`use-task-watch.ts` 里就是这么做的。
- 写完 effect 后跑 `pnpm lint`、warning 不能留。

## 减少手戳代码 / 优先复用 + 用成熟库（用户拍板）

**核心思想**：能复用就别复制粘贴、能调库就别手撸、能下沉就别上抛。任何看起来「state / handler / 工具函数 / 类型 / 逻辑 / UI 块」在两处以上长得很像、或者你正在写第 50+ 行手工状态机——立刻停、想想能不能抽 / 调库。

### 减少 state

- **`useState` 数量是复杂度指标**——一个组件超过 5-6 个 useState 就该警惕：
  - **多个相关 state 合并**：用 `useReducer` 或对象 state（如 `useState<{ open: boolean; draft: string; submitting: boolean }>`）
  - **派生 state 不要存**：能 `useMemo` / 直接计算的就别 `setState`、避免「state 漂移 + 多次 effect 同步」
  - **state 下沉**：父组件不需要的 state（如 dialog 内部 draft）下沉到子组件、父只持开关、参考 `revise-dialog.tsx` 的拆法

### 减少重复代码 / 减少手戳方法

- **同样的 handler 写两遍 → 抽 hook 或 lib**——「贴图处理」「SSE 订阅」「fetch + 错误归一」这种、第二次写就该抽。本仓库现成例子：`use-task-watch.ts`（SSE 订阅）/`use-image-attach.ts`（图附件）/`task-store.ts` 的 `handleJson`/`run-args.ts` 的 `prepareRunArgs`。
- **同样的 UI 块写两遍 → 抽组件**——参考 LoadingState / EmptyHint / ChoiceButton 一节。
- **业务工具函数（formatXxx / parseXxx / extractXxx）→ 抽到 `src/lib/*.ts`**——别在组件里 inline、别在多个组件各自实现一份（典型反例：以前 STATUS_LABEL 在 task-card 和 artifact-panel 各定义一份）。
- **方法体超过 30 行 / 嵌套 > 3 层 / 单函数 setState > 3 次** → 拆。要么拆成更小的 helper、要么进 useReducer / 进 hook。
- **避免手撸状态机**——例如「拖拽：dragenter / dragover / dragleave / drop + 4 个边界 case + isDragging state」、「图片上传：file input ref + paste handler + drop handler + size 校验 + base64 编码 + 缩略图 state」——这些都该是一个 hook 包住、调用方一行。

### 优先用成熟库

- **表单 / 弹窗输入** → `react-hook-form`、别手动 `useState(draft) + onChange + validate + submit` 拼。
- **服务端状态** → `@tanstack/react-query`、别手动 `useState(data) + useEffect(fetch) + useState(loading) + useState(error)` 那套 5-state 笨办法（项目目前还在裸 fetch、新功能优先考虑引入）。
- **不可变更新** → `immer` / `use-immer`、嵌套对象的 `setState(prev => ({...prev, foo: {...prev.foo, bar: x}}))` 链式展开换成 `draft.foo.bar = x`。
- **拖拽 / 手势** → `@use-gesture/react` + `framer-motion`、别手撸事件处理。
- **日期 / 时间** → 简单格式化（「N 分钟前」/「MM-DD HH:mm」这种）直接 `new Date()` + `padStart` 拼即可、参考 `src/lib/task-display.ts` 的 `formatRelative` / `buildDefaultChatTitle`。如果出现复杂时区 / 跨地区 / 多 locale 需求再按需引 `dayjs` / `date-fns`、目前 package.json 里没装、不要凭空 import。
- **写之前先扫一眼 `package.json`**——已经有的库直接用、别绕开。

### 抽象门槛

- **复用次数 >= 2 且能省至少 30 行手工代码** → 值得抽 hook / 组件 / lib。
- **临时性 / 一次性 / 项目特定 < 30 行** → 内嵌、不抽（强行抽反而增加跳转成本）。
- **修了同一类 bug 三次** → 一定要抽出来集中处理（典型反例：早期每个 fetch 都自己拼 4xx 错误 toast、最终抽 `handleJson<T>` 统一）。
- **例外：「典型 UI 组件」即使当前只有 1 个 caller、也应该抽到 `src/components/ui/`**（用户拍板、V0.5.9 立规）
  - 判断：组件是「业界有共识的标准件」吗？（Select、Combobox、MultiSelect、DatePicker、Tabs、Toast、Tooltip 等）
  - 这类组件大概率以后会复用、即使当下不复用、抽出来：(1) 调用方更清爽；(2) 视觉契约 / 行为单一来源；(3) 别人新加场景时不会拷一份；(4) 跟 shadcn/ui 风格对齐方便集中维护
  - 反例：`RepoMultiSelect` 第一稿放 `new-task-dialog.tsx` 内部、用户反馈「这种典型的组件、肯定是要抽出来的、不要刻板的遵守规则」、当天抽到 `src/components/ui/multi-select.tsx` generic 版

## Dialog 内长内容溢出（V0.5.9 hot-fix 沉淀）

**问题**：dialog 里贴超长字符串（无空格的日志 / URL / 序列化 JSON）、内容撑破 dialog 右边界、`truncate` 不生效。

**根因**：CSS flex/grid item 默认 `min-width: auto` = intrinsic min-content（= 内容宽度）、长字符串撑大父容器。

**项目已做的统一兜底**（不需要 dialog 调用方自己加）：

- `src/components/ui/dialog.tsx`：`DialogContent` 默认带 `*:min-w-0`——所有 grid item（DialogHeader / DialogFooter / 调用方塞的 div / textarea wrapper 等）自动获得 `min-w-0`、不会被内容撑破 dialog max-w
- `src/components/ui/textarea.tsx`：默认带 `min-w-0 max-w-full wrap-anywhere`——field-sizing 不再撑父、无空格长串能折断

**调用方仍需注意**：

- **`truncate` 必配 `min-w-0`**——dialog 兜底只覆盖第一层 grid item、嵌套 flex/grid 的 truncate 仍要自己加。
  ```tsx
  <span className="truncate flex-1 min-w-0" title={...}>{longText}</span>
  ```
- **`wrap-anywhere`**（Tailwind 4 = `overflow-wrap: anywhere`）让无空格长字符串能折断、配合 `<pre>` / 自定义 mono 文本块用

## review / bug 排查：先验证事实再下结论（V0.6.1 review 踩过、用户拍板）

- **不要凭推理 / 凭记忆下 bug 结论**——读真实代码 / 配置 / 数据确认后再说。典型反例：V0.6.1 review 判定 `checkShip` 正则会漏检、却没读 `action-ship.md` 的格式约束、其实 prompt 强制了表格（repoTail 在前 / 跳过在后）、forward 正则能命中、bug 不成立。
- **构造的假想例子必须明标「构造」、不当证据**——举例说 bug 场景（如「agent 写『跳过 crm-web』会漏检」）要讲清是假想、不是从真实 artifact 读到的。把假想例子当证据 = 高估 bug。
- **被 challenge 后第一反应是回头核实、不是辩解**——用户质疑某条结论时、先读真实代码 / 数据验证、错了就明确更正 + 降级优先级。
- **方法论（蓝军 + RCA）**：读真实代码确认行号逻辑 → 主动证伪「这是 bug」→ 攻不破才是真 bug → 标注触发条件 + 概率。
- **grep 本仓库没命中 ≠ 现象不存在**（V0.7.19 连踩两次、用户实证纠正）——外部依赖 / IDE / SDK 层的现象（如 Cursor 内置 anti-loop 提示「flagged as looping」、composer 2.5 运行时把长 shell 自动转后台），本仓 grep 必然找不到、但真实存在。别把「自己代码里没搜到」当「是 agent 编的 / 不存在」（踩坑：把 SDK 自动转后台渲染出的第 2 条 curl 臆断成「引导诱导重发」、把 Cursor 真 anti-loop 提示误判成 agent hallucinate）。下结论前先问「这现象可能来自哪一层」、验证范围别只圈自己代码库。

## 飞书项目官方 MCP：远程服务、行为会无预警变更（2026-06-12 踩过）

- `project.feishu.cn/mcp_server/v1` 是官方远程服务、无版本锁定——**同样参数 06-09 成功、06-12 报错**（add_comment mention 从 lark_user_id 体系切到 user_key 体系、报 `userKey cross tenant`）是真实发生过的。遇到「之前 OK 的飞书项目 MCP 调用突然挂」、先对照历史成功调用的参数（task events.jsonl 里有完整记录）、参数没变 = 服务端变了、做对照实验确诊、别先怀疑自己代码。
- **@ 通知实验不要拿自己当被 @ 对象**——飞书项目全系「降噪不通知操作人自己」（官方 FAQ 明文）、MCP 走你本人 OAuth、@ 自己永远收不到通知、实验结果无效。要找第二个人验证送达。
- 两套 id 体系别混：`user_key`（飞书项目体系、role_members 的 member.key、search_user_info 的 user_key 字段）vs `lark_user_id`（飞书 IM 体系）、都是 19 位纯数字、肉眼分不出来、写 artifact / 存字段时必须标清是哪种。
- **add_comment 的 @ 推送是「回归 bug」、不是 by-design（2026-06-16 第二人复测确认仍坏）**：早先误判成「API 写不推送是设计如此」、收回。官方文档 [飞书项目需求详情页](https://www.feishu.cn/content/4ppsquwn) 明文「评论中@人（邮箱前缀）、被@的人将收到私信通知」——即评论 @ **本该**发私信。用户实证「上周（~06-09）还能用、评论改版后（~06-12）坏」。复现：`add_comment` 分别用 lark_user_id 体系（mention id=`lark_user_id_*` + notify_user_type=lark_user_id）和 user_key 体系（mention id=`user_key_*` + notify_user_type=user_key）发评论、**第二人肖康两条都没收到私信**；连 `update_field`/`update_node` 改角色 / 改活跃节点 owner（注：「测试排期」节点 owner 实际绑 QA 角色、update_node 的 node_owners 是 **add 增量语义**、移除要用 update_field role_operate remove）也没推送。手动 UI（走邮箱前缀、前端识别）不受影响。**公开渠道暂无该回归的官方公告 / 社区吐槽 / workaround、用户决定等官方修**。要立刻能通知只能 app 侧绕：接飞书自建机器人 `/open-apis/im/v1/messages` 直发 IM（需自建应用 + 消息权限 + 被通知人在可见范围）。下次别再从头排查、直接看这条；想确认是否已修、按上面复现法拿第二人验一次即可。

## MCP 工具 schema 的 describe 要和 prompt 指令一致（V0.6.1 踩过）

- **工具 `inputSchema` 的 `.describe()` 是 agent 会读的「第二指令源」**——跟 prompt 冲突会把 agent 带偏。典型反例：`submit_mr` 的 `target_branch` describe 写「探 `origin/HEAD` 拿 master」、但 `action-ship.md` 写「写死 `test`」、两个矛盾指令可能让 agent 提 MR 到错误分支。
- 改 prompt 里某工具的用法约定时、**一并核对该工具在 `chat-mcp.ts` 注册处的 describe**、别让两边漂移。

## 不要把「用户偏好级」规则写死进产品 prompt（2026-06-16 用户拍板）

- **语言（中文 / 英文）、语气、个人风格这类「用户偏好」不该硬编码进 `_super.md` / `_shared.md` / chat prompt**——用户的 Cursor 全局 user rule（`~/.cursor/rules/`）已经由 `skills-loader` / rulesSection 注入进 prompt 了（如「思考和回复永远用中文」就是从这来的）。写死 = 剥夺灵活性（万一有用户偏好英文呢）。
- **判断闸门：「这是我们代码的 bug、还是用户偏好没传到 / 模型没遵守？」**
  - 是**我们代码的 bug**（如全局规则压根没注入、注入顺序错、被截断）→ 修我们的代码。
  - 是**用户偏好**（语言 / 风格）→ 不在产品 prompt 里写死、交给注入的 user rule 承载；模型在英文基座上下文里偶尔不遵守是模型层问题、不靠我们硬塞偏好来「治」。
- 反例（2026-06-16 差点踩）：线上 task agent 旁白掉英文、本想往 `_super.md` 顶层加一条「所有 narration 永远中文」硬规则——被用户拦下：语言是偏好、全局 rule 已注入、不写死。
- 推论：要提升注入的 user rule 的「权重 / 位置」（如 front-load）是**语言无关的中性改进**、可单独评估；但别把改进做成「专门写死某个具体偏好」。

## task-scoped handler / notifier 注销用 conditional unset（V0.6.1 踩过）

- chat-mcp 的 `taskActionHandler` / `awaitingNotifier` 按 taskId 存单例 Map、force-new-agent 时新旧 agent 共享同一 key。
- 旧 agent finally **注销前比对实例引用**（`if (map.get(taskId) === expected) map.delete(taskId)`）、只清「自己注册的那个」——否则旧 agent 迟到的 finally 会误清新 agent 刚注册的 handler、submit_mr 就拿不到 handler。
- 用 `unsetChatTaskActionHandlerIf(taskId, expected)` / `unsetChatAwaitingNotifierIf`、不要无条件 `set(taskId, null)`。

## 改代码前先 feedback 确认方案（2026-06-10 用户拍板）

- **任何代码改动动手前、先把「准备怎么改」（改哪些文件 / 思路 / 影响面）通过 cursor-feedback 发给用户确认**、用户点头后再写代码。
- 例外：用户已经在反馈里明确说「可以、动手」的当次改动、不用再确认一遍。
- 排查 / 只读分析不受限——读代码、跑测试、复现问题都可以直接做、只有「写改动」要先过确认。

## 发版 push 完即报、不用守 CI（2026-07-08 用户拍板）

- tag push 出去就通过 feedback 告知用户、**不要挂着轮询等 CI 跑完再汇报**——用户自己会看进度。
- 例外：用户明确要求「盯着 / 跑完告诉我」、或发版链刚改过（如压缩配置 / 打包脚本变更）首次验证时、才有必要确认产物落地。

## 发版要先问、小改动不单独发（2026-06-18 用户拍板）

- **发版（打 tag + push 触发 CI 打包）前必须先问用户确认**——发版不是「写完改动」的自然延续、是独立动作、要单独征求同意。
- **一次性发版授权 ≠ 持续授权**：用户对「某个改动」说「直接上 / 发吧」只对**那一次那个改动**有效、**不自动延续到后续新改动 / 新版本**。每个新 tag 都要重新确认。
- **小样式 / 小改动不必马上单独发版**——可以攒着、和后续改动合并到下一个版本一起发、别一个 className 改动就占一个版本号。「值不值得马上上线」由用户决定、不要替用户拍板。
- 踩坑（2026-06-18）：用户「检查下没问题直接上」是针对 v0.8.12 功能修复；我修完一个新冒出来的小样式 bug（批次表状态列换行）后把那次授权当持续授权、又自作主张 tag v0.8.13 直接发——用户「发版你要问下我啊、这种小样式改动不需要马上发的」。

## shell 工具调用避坑（2026-06-13 屡次空返回 exit 1 踩过）

本仓库验证桌面 app 常跑含中文 app 名「AI工作流test」的 shell——两个 zsh / pkill 特性会让命令静默空返回 exit 1、后续 `;` 命令全不执行、看起来像「工具调用失败 / 不长记性」、其实是命令本身被中断：

- **zsh nomatch**：命令里的 glob（如 `ls ~/Library/Logs/*工作流*`）**不匹配时 zsh 直接报错中断整条命令**（不像 bash 把字面量传下去）、`;` 后的命令一个都不跑。规避：别用裸 glob 探路径、用 `ls 目录/ 2>/dev/null` 再人眼找、或 `setopt null_glob`。
- **pkill -f 自匹配杀自己**：`pkill -f "AI工作流test"` 的 `-f` 匹配**完整命令行**、而执行这条命令的 shell 自身命令行就含「AI工作流test」→ 把自己 SIGKILL 了（空返回 exit 1）。规避：① 杀进程优先 `osascript ... quit`（既有约定）；② 非要 pkill 用经典字符类技巧 `pkill -f "[A]I工作流test"`（正则 `[A]` 匹配 A、但命令行字面是 `[A]I…` 不含连续「AI工作流test」、不自杀）、且 pkill 单独成命令、别和含「AI工作流test」字面的 ls / 路径混在一条。
- **工具 shell 跑在 errexit + pipefail 下（2026-06-15 连踩、空返回 exit 1）**：任何**独立**命令 / 管道返回非 0 就**中断整条**、后续行全不执行（看着像「卡住」）。最常见肇事者是「无匹配即退 1」的命令：`grep`/`grep -c`（0 命中退 1）、`lsof`（端口空退 1）、`pgrep`（无进程退 1）。规避：这类命令一律 `… || true` 兜底、或用 `if`/`$( … || echo 默认)` 包住；放在 `&&`/`||` 链里的非末命令不受 errexit 影响（这是 set -e 的豁免、可利用）。
- **`rg` 不在工具 shell 的 PATH（2026-06-15 踩过、假阴性最坑）**：shell 里 `rg` 直接 `command not found` 退 127——而我若写 `rg … || echo "未找到"` 会把「rg 没装」误读成「没搜到」、得出错误结论。仓库内搜一律用 **Grep 工具**（基于 ripgrep、已优化）；非要在 shell 里搜用 `grep`（系统自带、可靠）。别在 shell 里调 `rg`。
