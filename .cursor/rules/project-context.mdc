---
alwaysApply: true
description: fe-ai-flow 项目核心上下文、新对话第 1 步必读
---

# fe-ai-flow 项目核心上下文

## 项目定位（V0.2 重定、V0.3 持续打磨）

**fe-ai-flow = 站在 Cursor 肩膀上的「企业 / 项目级 AI Harness 平台」。**

不是再造一个 Cursor / Claude Code、是给前端项目（一开始针对 crm-web 这种业务仓库）做一层「需求 → PR」的任务自动化壳：

- 把「读飞书 / 拉接口文档 / 摸代码 / 写技术方案 / 提 PR / 同步状态」这 80% 的手工活自动化
- 写代码也用 SDK Agent 自己跑、**完全脱离 Cursor IDE**
- 每一步通过 harness 工具（typecheck / lint / hooks / Skills / MCP）加固质量

## 设计权威源：飞书文档

**[fe-ai-flow 设计（V0.2 草稿）](https://wukongedu.feishu.cn/docx/Ue7Td9mOWoU57cxVctfcLA68nic)**

- 完整定位 / 主场景预演 / 4 张架构图 / 12 步 harness 方法论 / 路线图 / 设计原则
- 项目里所有 docs/*.md 都不是权威源、以飞书为准
- 接力上下文最少要先打开飞书文档读一遍
- 注意：飞书文档标题写的是 V0.2、但代码已经演进到 V0.3.5、phase 模型 / 保活机制都跟飞书文档不一致、以代码 + `docs/HANDOFF.md` 为实际权威

## 关键约束（不要踩雷）

- **HITL 是底线**——别提"全自动"、所有 phase 之间必须人 ack
- **单 SDK Run**：整条 workflow（plan → build）跑在一个 Cursor SDK Run 里、phase 间阻塞等用户 ack（不每 phase 起新 Run、省 Cursor 计费）
- **保活走 shell + curl long-poll**（V0.3.5）：`wait_for_user` / `ask_user` MCP 工具立即返回 shell 引导、agent 调 `shell` curl 一条长 HTTP 连接到 `/api/tasks/[id]/wait-ack`、服务端阻塞、用户 ack 时 resolve promise → 写一行结果 → 关流 → agent 推进。**不**用 keep_alive_a/b/c 那套（已删）。
- **不接 LangChain / LangGraph / Mastra / CrewAI**——保持简单
- **思考和回复永远用中文**
- **每次对话操作前唤起 cursor-feedback、timeout 600 秒**
- **代码改完跑 `pnpm typecheck`**——用户对低级错误零容忍
- **开发期不写向后兼容代码**：改 schema / 字段名 / EventKind 枚举 / 接口签名时、直接删旧的、不写双兼容

## V0.3.4 phase 模型（当前）

```
plan → build
```

- **plan**：读 contextDocs（飞书 story + 用户补充文档）+ 扫本地仓库 + 出技术方案 → `artifacts/01-plan.md`
- **build**：SDK Agent 按 plan 写代码 + 跑 hooks → `artifacts/02-build.md`

历史演进：
- V0.2 是 `context → plan → build → ship`（4 phase）
- V0.3.3 砍了 ship（提 PR + 同步飞书 story 状态、效果不稳）
- V0.3.4 把 context 合进 plan（实操中分离价值未兑现、用户审 context 跟审 plan 判断点重合、合并后 plan 一气呵成 1 次 ack）

V1 legacy 模式（chat-only）的代码保留、新功能不再往那边加。

## 技术栈

- Next.js 15 + React 19 + Tailwind 4
- `@cursor/sdk` 1.0.13（Beta、2026-05-14 从 1.0.10 升）
- `@modelcontextprotocol/sdk` 1.29.0（HTTP MCP transport）
- shadcn/ui base-nova（@base-ui/react、不是 Radix）
- TypeScript strict
- localStorage 配置（API key + 模型 + 仓库 + MCP）

## 用户协作风格

- 偏好客观、数据、业界共识
- 不喜欢「真·multi-agent 协作」/ 全自动 这种"颠覆性"幻想；workflow + 多 phase chain 是 OK 的
- 中文沟通
- 错别字 / typo 零容忍
- 喜欢分阶段验证 ROI、不一次性投入
- 容易摇摆、需要"先在飞书文档对齐再动代码"

更细节看 `docs/HANDOFF.md`（里面有指向飞书的链接）。
