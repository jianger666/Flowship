---
alwaysApply: true
description: ai-flow 项目核心上下文、新对话第 1 步必读
---

# ai-flow 项目核心上下文

## 项目定位（V0.2 重定、V0.3 持续打磨）

**ai-flow = 站在 Cursor 肩膀上的「企业 / 项目级 AI Harness 平台」。**

不是再造一个 Cursor / Claude Code、是给前端项目（一开始针对 crm-web 这种业务仓库）做一层「需求 → PR」的任务自动化壳：

- 把「读飞书 / 拉接口文档 / 摸代码 / 写技术方案 / 提 PR / 同步状态」这 80% 的手工活自动化
- 写代码也用 SDK Agent 自己跑、**完全脱离 Cursor IDE**
- 每一步通过 harness 工具（typecheck / lint / hooks / Skills / MCP）加固质量

## 设计权威源

**代码 + `docs/HANDOFF.md`**——其余 docs/*.md（DESIGN / ROADMAP / MULTI-ROLE / PRODUCT-COMPARISON 等）为辅助、有冲突以代码 + HANDOFF 为准。

### 文档分工（V0.5.11 起、避免 HANDOFF 越撑越大）

- **`docs/HANDOFF.md`**（接力第一文件、~300 行）：项目定位 + 最小上下文 + 代码层面要点 + **当前架构快照**（V0.6 系列稳定描述、不带版本号）+ **最近演进**（窗口式、保留最近 2 个子版本）+ 关键文件索引
- **`docs/CHANGELOG.md`**（历史档案、~2000 行）：V0.2 ~ 最新前两版的全部演进细节、时间倒序（新在上）

### 设计层面变动「下次记账规则」

1. **当前架构变动**（如 action 模型改、保活机制改、新增大组件）→ 改代码 + 同步更新 `HANDOFF.md`「当前架构快照」段
2. **小步迭代**（同主题连续 .1 / .2 / .3 微调）→ 改代码 + 写到 `HANDOFF.md`「最近演进」段顶部
3. **再老一轮时**（「最近演进」积压超过 2 个子版本）→ 把最老那段迁到 `docs/CHANGELOG.md` 顶部、HANDOFF.md 始终保持 2 个子版本窗口
4. ⛔ **不要**散落到其它 md 写一份新的演进段
5. ⛔ **不要**往 HANDOFF.md 中间段插新版本（中间段是「当前架构快照」、不带版本号、改了一定是架构级变动、追加到段尾或刷新对应字段、不是「插一个 V0.X.Y」）

## 交付形态：Electron 桌面 app 唯一（2026-06-12 用户拍板）

- **后续不用考虑网页版、绿色版**——桌面 Electron app 是唯一交付形态。
- 新功能一律按桌面端设计（原生 picker / 壳 IPC / 自更新等能力随便用）、**不为浏览器场景做适配或兼容优化**。
- 绿色 zip 包 + 便携 node + launcher + CI 绿色包 job 已彻底清理——唯一发版链 = Electron 安装包（`packaging/` 仅留 `icon.png` 做图标源）。

## 关键约束（不要踩雷）

- **HITL 是底线**——别提"全自动"、所有 action 之间必须人 ack
- **会话模型（V0.11 起「create + 多轮 send」、wait 协议已退役）**：agent 交卷（`wait_for_user` 非阻塞）/ 提问（`ask_user` 非阻塞）后**自然结束 turn**；用户操作（推进续用 / 再聊聊 / ask 答案 / chat 消息）由 server `agent.send(新消息)` 续同一会话（`agentSessions` 跨 run 存活）。旧「单 Run 永生 + shell curl 长轮询 wait-ack」机制已删（Cursor 取消按次计费 + worktree 隔离后前提消失）、别再按老协议改代码 / prompt。
- **不接 LangChain / LangGraph / Mastra / CrewAI**——保持简单
- **思考和回复永远用中文**
- **每次对话操作前唤起 cursor-feedback、timeout 600 秒**
- **等子代理/长任务时不要用 AwaitShell 挂倒计时干等（2026-07-24 用户两次纠正）**——等待期间一律唤起 cursor-feedback 挂着（用户随时能插话）；子代理完成靠 end-of-turn 通知回来处理
- **代码改完跑 `pnpm typecheck` + `pnpm lint`**——用户对低级错误零容忍、warning 都不能留
- **开发期不写向后兼容代码**：改 schema / 字段名 / EventKind 枚举 / 接口签名时、直接删旧的、不写双兼容

## V0.6 task 容器 + action 历史模型（当前）

V0.5 phase chain（`plan → build → review` 固定顺序）已废弃、V0.6.0 重构为 **task 容器 + action 历史**：

- **task** = 单个需求生命周期容器、双状态（`repoStatus` 业务状态 + `runStatus` agent 运行时状态）
- **action** = 单次动作、6 + 1 种类型：`plan / build / review / ship / test / learn / chat`、任意触发不强制顺序
- **artifact** 路径：`data/tasks/<id>/actions/<n>-<type>.md`、N 单调递增

V0.6.0 已实装：plan / build / review / chat；V0.6.1+ 上 ship / test / learn。

V0.5 兼容代码 / 数据彻底删（不写 migration、开发期接受重置 data/tasks/*）。

历史演进细节看 `docs/CHANGELOG.md` + `docs/V0.6-REFACTOR.md`（设计文档、已 archived）。关键转折点：
- V0.2 4 phase `context → plan → build → ship` → V0.3.3 砍 ship → V0.3.4 context 合进 plan → V0.5 重新加 review → V0.6.0 拆 phase 顺序、改 action 历史模型

## 技术栈

- Next.js 15 + React 19 + Tailwind 4
- `@cursor/sdk` 1.0.23（Beta、2026-06-17 从 1.0.17 升）
- `@modelcontextprotocol/sdk` 1.29.0（HTTP MCP transport）
- shadcn/ui base-nova（@base-ui/react、不是 Radix）
- TypeScript strict
- localStorage 配置（API key + 模型 + 仓库 + MCP）

## 用户协作风格

- 偏好客观、数据、业界共识
- 中文沟通
- 错别字 / typo 零容忍
- 喜欢分阶段验证 ROI、不一次性投入
- 容易摇摆、需要"先把变更落到 docs/HANDOFF.md 演进段再动代码"

更细节看 `docs/HANDOFF.md`（里面有指向飞书的链接）。
