---
alwaysApply: true
description: fe-ai-flow 项目核心上下文、新对话第 1 步必读
---

# fe-ai-flow 项目核心上下文

**身份**：我是 `fe-ai-flow`、用户的私人项目、定位为公司前端项目通用自动化工具。

## 必读文档（按优先级）

1. `docs/HANDOFF.md` — 给 AI 对话的接力上下文、必读
2. `docs/DESIGN.md` — 关键设计决策、改任何架构前必读
3. `docs/ROADMAP.md` — 阶段路线图、决策"做什么"前必读
4. `README.md` — 用户视角的运行说明
5. ~~`prompts/build-spec.md`~~ — V0.1 已 clean slate、目录已删、未来重建

## 项目根本方法论：Harness（最重要）

**本项目就是 harness 工程化方法论的实操落地——所有架构决策、迭代建议都必须在 harness 框架内进行。**

- 看 `docs/HANDOFF.md`「项目根本方法论：基于 Harness」章节是必读项
- 加新能力之前先问：它对应 harness 哪个组件（角色 / 工具 / 记忆 / 流程 / 守门 / 观测 / 评估）？解决什么 reliability gap？
- 业界共识来源排序：Anthropic *Building Effective Agents* > Cognition *Don't Build Multi-Agents* > 真生产案例（Copilot Workspace / Cursor Cloud / Aider / Replit）> 《AI Coding 工程化》（参考、不照搬）> 凭直觉
- 用户问 harness 时不要套那本书的"五元素"、给业界主流答案

## 关键约束（不要踩雷）

- **HITL 是底线**——别提"全自动"、所有 phase 之间必须人 ack
- **Workflow + 多 phase chain**——单 agent 全程 / 每 phase 独立 agent **都要支持**、用户已拍板「可单可多」。**注意**：Cognition 反对的是「真·multi-agent 协作」（角色谈判 / 共享记忆、如 BMAD），不反对 phase chain 里多个 agent 节点 + 文件 hand-off
- **不要建议接 LangChain / LangGraph / Mastra / CrewAI**——保持简单
- **思考和回复永远用中文**
- **每次对话操作前唤起 cursor-feedback、且 timeout 600 秒**
- **代码改完跑 `pnpm typecheck`**——用户对低级错误零容忍

## 当前状态（clean slate 后）

- ✅ 基础设施：Next.js 15 + React 19 + Tailwind 4 + shadcn/ui base-nova、强制 dark
- ✅ 设置页：API Key / 默认模型（含 SDK 参数）/ 仓库（原生 dialog + 手填备份）/ MCP servers JSON
- ✅ 已 push 到 https://github.com/jianger666/fe-ai-flow（private）
- 🚧 主流程交互（首页）——形态待和用户讨论
- 🤔 spec / plan / build 三段式——保留参考、是否再走待用户拍

## 技术栈

- Next.js 15 + React 19 + Tailwind 4
- `@cursor/sdk` 1.0.10（Beta）
- shadcn/ui base-nova（@base-ui/react、不是 Radix）
- TypeScript strict
- localStorage 配置（API key + 模型 + 仓库 + MCP）

## 待决策项（用户没拍板的）

- 主页交互形态（表单 / 聊天 / 混合）
- 单 agent 全程 vs 每 phase 独立 agent 的 UI 入口怎么暴露
- 按 phase 配模型——**用户明说「先把流程跑通再考虑」、本轮不做**
- 是否接飞书 MCP 自动拉文档
- 是否要 cost / token dashboard

## 用户协作风格

- 偏好客观、数据、业界共识
- 不喜欢「真·multi-agent 协作」/ 全自动 这种"颠覆性"幻想；workflow + 多 agent 节点是 OK 的
- 中文沟通
- 错别字 / typo 零容忍
- 喜欢分阶段验证 ROI、不一次性投入

更细节看 `docs/HANDOFF.md`。
