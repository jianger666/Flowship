node_modules
.next
out
build
dist

.env
.env.local
.env.*.local

# Persisted task data (per-user)——根锚定、别误伤子目录里叫 data 的（如 .cursor/skills/*/data）
/data/

# Python 编译缓存（skill 脚本会产生）
__pycache__/

# TS incremental cache
*.tsbuildinfo

# Vercel
.vercel

.DS_Store
*.log

# 真机冒烟调试产物（含真实 openId/chat_id 等标识、公开仓不入库）
scripts/.smoke-*.json
